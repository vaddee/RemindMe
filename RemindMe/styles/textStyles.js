import { StyleSheet } from 'react-native';

const textStyles = StyleSheet.create({
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 16,
    color: '#1E90FF', // Sininen 
  },
  subHeader: {
    fontSize: 18,
    fontWeight: '600',
    marginVertical: 8,
    color: '#333', // Tummanharmaa 
  },
  bodyText: {
    fontSize: 16,
    marginBottom: 4,
    color: '#444', // harmaa 
  },
  highlight: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#1E90FF', // Sininen 
  },
});

export default textStyles;
